name = "danmaku-relay"
main = "worker.js"
compatibility_date = "2026-05-01"

# Durable Object のバインディング
[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# 無料プランは SQLite バックエンド必須 → new_sqlite_classes を使う
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
